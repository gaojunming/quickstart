package cn.bugcatch.quickstart.dao;

import org.mybatis.spring.annotation.MapperScan;

//@Mapper
public class MyDao {
	//@Select("SELECT * FROM CITY WHERE state = #{state}")
    //Object findByState(@Param("state") String state);
}
